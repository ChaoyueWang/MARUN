MARUN程序

1.	READIN.IN 文件中添加，类似应力期的参数。针对MARUN的运行结果输出，可以设置一个时刻，在该时刻以前按某一时间步长输出。在该时刻以后按另一个时间步长输出，这样可以避免产生过多的输出结果，并且可以得到与初始条的连续变化。具体设置如下：在READIN.IN 文件中的dt_write 前增加两个参数：dt_change, dt_write0。dt_change 即为输出时间步长改变的时刻。Ttime<dt_change 时，输出时间间隔为dt_write0, ttime>dt_change 时，输出时间间隔为dt_write。这样做的好处是可以避免产生大量输出结果。特别是有观测数据时，右边界也是海潮观测数据，已经扩展到10年，我们最想要的只是最后15天，每半小时间隔的运行结果。那我们十年前的运行结果可以设置为1年输出依次。这样节约了大量存储空间。同时也避免程序中途中断，可以保存中间某年的运行结果。 
2.	当TidalP.IN 中Hrms不为0时，即为考虑海浪的模拟情况。MARUN程序会输出新的输出文件wavelevel，用于存储各输出时刻被海水浸没结点的水位，程序中关于海浪的设置为相位平均的结果。所以海水位曲线为向陆倾斜的一条曲线。
MATLAB处理程序

1.	批量自动先后处理程序运行结果。文件夹的个数大于等于1。首先判断有无观测数据，依据为在各种case的同级文件夹下有没有observed.IN 文件，然后依次进入各个文件夹，录入各种数据。READIN.IN, NODE2D.IN, ELEM2D.IN, BOUNDPRIT.IN, TidalP.IN, MATERIA.IN, INIT.IN,PIEZO, ‘MARUNOUT’, 如果有海浪还会导入各输出时刻的海水的水位文件“wavelevel”。
2.	处理程序中包含一些子程序，当确定不需要输出某些结果时，可以讲该子程序注释掉。
3.	每个文件夹的处理结果作图、输出文件，都会放到该文件夹下。
4.	若同时处理的文件夹多于一个，将会把共同的压力水头，盐度提取出来，生成的工作区保存在外层。然后用作图程序作图比较。
